apiVersion: v1
clusters:
  - cluster:
      server: https://your-k8s-cluster.com
    name: cluster
contexts:
  - context:
      cluster: cluster
      user: user
    name: context
current-context: context
kind: Config
preferences: {}
users:
  - name: user
    user:
      token: secret
